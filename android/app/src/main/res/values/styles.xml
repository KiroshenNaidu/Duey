<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Dark window background so nothing light shows between splash dismiss and WebView paint. -->
        <item name="android:background">@color/splashBg</item>
        <item name="android:windowBackground">@color/splashBg</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!-- Plain dark splash (the web mp4 carries the branding). Covers both the
             Android 12+ system splash and the legacy window background. -->
        <item name="windowSplashScreenBackground">@color/splashBg</item>
        <item name="android:windowBackground">@color/splashBg</item>
        <item name="android:background">@color/splashBg</item>
    </style>
</resources>